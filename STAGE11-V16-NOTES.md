# Stage 11 v16

- Favorite and compare icon controls added to car cards on homepage, catalog and related-car blocks.
- Hover tooltips are custom UI, while aria-label/aria-pressed remain accessible.
- Favorites are shown as a catalog-grid view instead of a separate favorites modal/page.
- Favorites state persists in localStorage and `favorites=1` can restore the catalog view.
- Mobile uses the same catalog-grid favorites view; no floating favorites panel is introduced.
