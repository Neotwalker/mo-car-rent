# Stage 11 v19

- Mobile sorting keeps the current `data-sort-label` visible instead of reducing the control to a chevron-only button.
- Mobile sort trigger/menu typography and spacing are reduced and the popup width is constrained to the viewport.
- Safari/iOS control appearance/color is normalized for sort, rental-date/calendar controls and vehicle gallery navigation.
- Vehicle single gallery Unicode arrows were replaced with inline SVG chevrons to prevent system-blue rendering on Apple devices.
