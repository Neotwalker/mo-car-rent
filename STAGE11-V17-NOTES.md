# Stage 11 v17

- Card container is no longer clickable; only the explicit «Подробнее» CTA opens the vehicle.
- Homepage card gallery receives pointer events, so hover preview works.
- Comparison is a catalog-grid saved view, parallel to Favorites, with localStorage persistence.
- Comparison modal/tray removed.
- Catalog result heading reduced.
