# Stage 11 v6 - catalog filters polish

- Price range restyled as one combined control and native number steppers hidden.
- Seats, brand, year, engine-volume bounds and consumption bounds use accessible custom selects with native fallback.
- Removed the modal section “Быстрые условия выбора”.
- Vehicle type cards use original project-owned SVG silhouettes rather than copied competitor assets; selected state has an explicit check mark.
- Catalog quick facets now use approved catalog vehicle photography rather than symbol icons.
- Added demo-only engine/consumption dataset attributes so the new controls are interactive in the static demo. These values are placeholders for CMS data, not business claims.
