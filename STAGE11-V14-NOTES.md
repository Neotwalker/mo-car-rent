# Stage 11 v14

- Mobile pickup and return place selectors are fullscreen sheets below 760px.
- Added dedicated mobile headers and close buttons to both place selectors.
- Body scroll locks while a mobile place selector is open.
- Filter modal mobile header is more compact.
- Filter close control is a true circular SVG button instead of the text multiplication glyph.
