# Stage 11 v21

Apple/Safari hardening:
- catalog calendar/filter buttons no longer inherit system blue;
- filter close SVG has explicit UI color;
- car-single and hero native date inputs keep semantics/functionality but Apple renders a separate centered visual date layer;
- empty Apple date controls show `дд.мм.гггг` through the same visual layer;
- literal escaped `\\n` sequences introduced in v20 CSS were normalized to real line breaks.
